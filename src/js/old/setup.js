var jquery_aleksi = $.noConflict();
var mode = 'plugin';
/*
var settings;
for (var setting in settings) {
    settings[setting] = chrome.storage.sync.get({setting: settings[setting]}, function(result) { return; });
}
*/
export { jquery_aleksi, mode };
